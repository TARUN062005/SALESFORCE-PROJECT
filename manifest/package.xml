<?xml version="1.0" encoding="UTF-8"?>
<Package xmlns="http://soap.sforce.com/2006/04/metadata">

    <!-- Custom Applications (Apps) -->
    <types>
        <members>*</members>
        <name>CustomApplication</name>
    </types>

    <!-- Tabs -->
    <types>
        <members>*</members>
        <name>CustomTab</name>
    </types>

    <!-- Custom Objects -->
    <types>
        <members>*</members>
        <name>CustomObject</name>
    </types>

    <!-- Apex Classes -->
    <types>
        <members>*</members>
        <name>ApexClass</name>
    </types>

    <!-- Apex Triggers -->
    <types>
        <members>*</members>
        <name>ApexTrigger</name>
    </types>

    <!-- Layouts -->
    <types>
        <members>*</members>
        <name>Layout</name>
    </types>

    <!-- Permission Sets -->
    <types>
        <members>*</members>
        <name>PermissionSet</name>
    </types>

    <!-- Flows (includes Process Builder and Flow Builder) -->
    <types>
        <members>*</members>
        <name>Flow</name>
    </types>

    <!-- Lightning Web Components (LWC) -->
    <types>
        <members>*</members>
        <name>LightningComponentBundle</name>
    </types>

    <!-- Email Templates -->
    <types>
        <members>*</members>
        <name>EmailTemplate</name>
    </types>

    <!-- Validation Rules -->
    <types>
        <members>*</members>
        <name>ValidationRule</name>
    </types>

    <!-- Record Types -->
    <types>
        <members>*</members>
        <name>RecordType</name>
    </types>

    <!-- Profiles -->
    <types>
        <members>*</members>
        <name>Profile</name>
    </types>

    <!-- Custom Metadata Types -->
    <types>
        <members>*</members>
        <name>CustomMetadata</name>
    </types>

    <!-- Static Resources -->
    <types>
        <members>*</members>
        <name>StaticResource</name>
    </types>

    <version>60.0</version> <!-- Use your org’s API version -->
</Package>
